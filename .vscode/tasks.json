{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/random_passcode_generator.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}